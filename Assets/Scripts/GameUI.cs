using UnityEngine;

namespace VertigoDemo.WheelOfFortune.UI
{
    public sealed class GameUI : MonoBehaviour
    {
        // Legacy placeholder.
        // Previous responsibilities were split into:
        // GameInputView, WheelView, ProgressBarView,
        // InventoryPanelView and ZoneInfoPanelView.
    }
}